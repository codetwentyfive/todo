{
  "name": "todo",
  "version": "1.0.0",
  "description": "To-Do List with Style",
  "main": "index.js",
  "scripts": {
    "build": "webpack",
    "start": "webpack serve --open",
    "test": "echo \"Error: no test specified\" && exit 1",
    "gh-deploy": "gh-pages --dist 'dist' --branch 'release'",
    "gh-deploy-init": "git push origin && git subtree push --prefix dist origin gh-pages"
  },
  "keywords": [
    "to-do",
    "list"
  ],
  "author": "codetwentyfive",
  "license": "ISC",
  "dependencies": {
    "webpack": "^5.88.2",
    "webpack-cli": "^5.1.4"
  },
  "devDependencies": {
    "css-loader": "^6.8.1",
    "style-loader": "^3.3.3",
    "webpack-dev-server": "^4.15.1"
  }
}
